/*
 * The <%= name %>Service is ...
 */

'use strict';

// We are safe to use fat arrow as we are registering this as a factory
export const <%= upCaseName %>Service = () => {
  const method = () => {
    return;
  };

  // Using ES6's object literal shortcut
  return { method };
};
